quarkus.http.port=8080

# Path to the folder containing DICOM files that form the patient roster.
# Can be overridden via environment variable ASSIGNMENT_ROSTER_PATH.
assignment.roster.path=test-data/roster

# Expose Swagger UI when running the packaged application (e.g. Docker container).
quarkus.swagger-ui.always-include=true
